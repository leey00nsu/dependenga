# Decisions Log

기술 결정과 그 이유를 기록합니다.

> 형식: `D{번호}: {결정 제목} ({YYYY-MM-DD})`

---

## D001: 모바일 패널 접힘은 로컬 상태 + 반응형 클래스로 처리 (2026-01-16)

- **Context**: 모바일에서 취약점 패널이 젠가를 가리는 문제 해결 필요
- **Options**: CSS만으로 숨김(토글 없음), 전역 상태/스토어로 관리, ResultView 로컬 상태로 토글 관리
- **Decision**: ResultView에서 로컬 상태로 접힘/펼침을 관리하고, 데스크톱은 `lg:` 브레이크포인트로 항상 펼침 처리
- **Rationale**: 구현 단순성, 상태 범위 최소화, 데스크톱 레이아웃 보장
- **Consequences**: 모바일 토글 버튼이 필요하며 상태는 새로고침 시 초기화됨

## D002: 데스크톱도 토글 허용, 기본값은 펼침 (2026-01-16)

- **Context**: 데스크톱에서도 패널을 접을 수 있어야 한다는 요구사항 추가
- **Options**: 데스크톱 항상 펼침 유지, 데스크톱 토글 허용(기본 펼침), 별도 환경 설정 제공
- **Decision**: 데스크톱에서도 토글을 허용하고 기본 상태는 펼침으로 설정
- **Rationale**: 데스크톱 사용자가 화면 공간을 선택적으로 확보할 수 있음
- **Consequences**: 토글 UI가 모든 뷰포트에서 노출됨

## D003: 패널 접힘 시 캔버스 영역 확장 (2026-01-16)

- **Context**: 데스크톱에서 젠가 캔버스가 잘려 보이는 문제 완화 필요
- **Options**: 항상 현재 패딩 유지, 패널 접힘 시 패딩 제거, 캔버스 자체 스케일 조정
- **Decision**: 패널 접힘 상태에서 캔버스 컨테이너 오른쪽 패딩을 제거해 시야 확장
- **Rationale**: 레이아웃 영향 최소화로 체감 개선
- **Consequences**: 패널이 접히면 캔버스가 패널 영역까지 확장됨

## D004: 캔버스 위치 고정으로 변경 (2026-01-16)

- **Context**: 패널 접기/펼치기 시 캔버스 이동이 의도와 달라 보인다는 피드백 발생
- **Options**: 패널 상태에 따라 캔버스 확장, 캔버스 위치 고정
- **Decision**: 데스크톱에서 캔버스 위치를 고정한다
- **Rationale**: 시각적 안정감 유지, 레이아웃 이동 최소화
- **Consequences**: 패널을 접어도 캔버스가 확장되지 않음
